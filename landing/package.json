{
  "name": "musicweb",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "echo 'ESLint not configured. To enable linting, run: npm install --save-dev eslint @typescript-eslint/parser'",
    "test": "echo 'Testing framework not configured. To enable testing, run: npm install --save-dev vitest'",
    "deploy": "echo 'Deployment not configured. Configure deployment platform (Vercel/Netlify/Render)'"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "18.3.5",
    "@types/react-dom": "18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.3",
    "vite": "^6.4.1"
  }
}
